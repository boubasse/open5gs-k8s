apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open5gs-amf.fullname" . }}-test-connection"
  labels:
    {{- include "open5gs-amf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open5gs-amf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
