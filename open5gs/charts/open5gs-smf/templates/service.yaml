apiVersion: v1
kind: Service
metadata:
  name: {{ include "open5gs-smf.fullname" . }}
  labels:
    {{- include "open5gs-smf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open5gs-smf.selectorLabels" . | nindent 4 }}
