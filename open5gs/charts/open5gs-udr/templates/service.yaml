apiVersion: v1
kind: Service
metadata:
  name: {{ include "open5gs-udr.fullname" . }}
  labels:
    {{- include "open5gs-udr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open5gs-udr.selectorLabels" . | nindent 4 }}
