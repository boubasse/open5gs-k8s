apiVersion: v1
kind: Service
metadata:
  name: {{ include "open5gs-ausf.fullname" . }}
  labels:
    {{- include "open5gs-ausf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open5gs-ausf.selectorLabels" . | nindent 4 }}
